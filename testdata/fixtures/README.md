# Provider fixtures

No approved sanitized-real response capture was available during the initial build. The repository therefore contains no successful provider fixture. The machine-checked status is in `../evidence/provider-fixtures.json`; `unavailable` does not certify provider compatibility and keeps the human release proof open. A designated human must capture, sanitize, scan, and review each compatibility fixture before it enters this directory.
